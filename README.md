# User Account Demo App

1. Create a .env file and set a variable called PORT to whichever port you would like to use.
1. Create a database and add it's connection string to .env
1. Run `node ./tools/seed.js` to configure the database.
1. Run `node main.js` to launch the application
